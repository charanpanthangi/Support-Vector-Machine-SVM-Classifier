# Examples

Run the main script to train and evaluate the Support Vector Machine classifier on the iris dataset:

```bash
python app/main.py
```

Artifacts such as the confusion matrix and PCA decision region plot will be saved to the `artifacts/` folder.
